{
	email 0xveya@gmail.com
}

uptime.saygex.xyz, status.saygex.xyz {
	reverse_proxy uptime-kuma:3001
}

# Future services should join the external `caddy` Docker network, then add:
# slots.saygex.xyz {
# 	reverse_proxy <slots-container-name>:<internal-port>
# }
